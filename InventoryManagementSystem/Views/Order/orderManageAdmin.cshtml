@*未審核訂單數量框 還沒歸還數量框

    待審核訂單/ALL/自己處理的/還沒歸還的(進行中訂單)/已經結束的*@
<link type="text/css" rel="stylesheet" href="https://unpkg.com/bootstrap/dist/css/bootstrap.min.css" />
<link type="text/css" rel="stylesheet" href="https://unpkg.com/bootstrap-vue@latest/dist/bootstrap-vue.min.css" />
<script src="https://unpkg.com/vue/dist/vue.min.js"></script>
<script src="https://unpkg.com/bootstrap-vue@latest/dist/bootstrap-vue.min.js"></script>

<div id="app">
    <div>
        <b-button variant="warning" class="font-weight-bolder">
            <img src="/images/timer_black_24dp.svg" />
            待審核訂單 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
        <b-button variant="success" class="font-weight-bolder">
            <img src="/images/lightbulb_white_24dp.svg" />
            待領取訂單 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
        <b-button variant="primary" class="font-weight-bolder">
            <img src="/images/outbox_white_24dp.svg" />
            租借中訂單 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
        <b-button variant="danger" class="font-weight-bolder">
            <img src="/images/new_releases_white_24dp.svg" />
            已逾期訂單 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
        <b-button variant="info" class="font-weight-bolder">
            <img src="/images/pending_actions_black_24dp.svg" />
            使用者申請報修 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
        <b-button variant="dark" class="font-weight-bolder">
            <img src="/images/announcement_white_24dp.svg" />
            使用者申請遺失 <b-badge variant="light" class="text-dark">4</b-badge>
        </b-button>
    </div>
        </br>
        <b-tabs active-nav-item-class="font-weight-bold text-uppercase text-danger"
                active-tab-class="font-weight-bold text-dark" content-class="mt-3" fill>
            <b-tab title="所有訂單" active>
                <b-navbar-nav class="ml-auto">
                    <b-nav-form>
                        <b-form-input size="sm" class="mr-sm-2" placeholder="Search"></b-form-input>
                        <b-button size="sm" class="my-2 my-sm-0" type="submit">Search</b-button>
                    </b-nav-form>
                    <hr/>
                    <template>
                        <div>
                            <b-table :items="items" :fields="fields" striped responsive="sm">
                                <template #cell(show_details)="row">
                                    <b-button size="sm" @@click="row.toggleDetails" class="mr-2">
                                        {{ row.detailsShowing ? '隱藏' : '顯示'}}
                                    </b-button>

                                    <!-- As `row.showDetails` is one-way, we call the toggleDetails function on @@change -->
                                    <b-form-checkbox v-model="row.detailsShowing" @@change="row.toggleDetails">
                                        Details via check
                                    </b-form-checkbox>
                                </template>

                                <template #row-details="row">
                                    <b-card>

                                        <b-row>
                                            <b-col><b>物品ID</b></b-col>
                                            <b-col><b>物品SN</b></b-col>
                                            <b-col><b>物品狀態</b></b-col>
                                        </b-row>
                                        <b-row class="mb-2 text-sm-right" sm="3" v-for="item in row.item.loop">
                                            <b-col>{{ item.itemId }}</b-col>
                                            <b-col>{{ item.itemSN }}</b-col>
                                            <b-col>{{ item.orderDetailStatus }}</b-col>
                                        </b-row>

                                        

                                        <b-button size="sm" @@click="row.toggleDetails">隱藏</b-button>
                                    </b-card>
                                </template>
                            </b-table>
                        </div>
                    </template>

                    <p>I'm the first tab</p>
</b-tab>
            <b-tab title="待核可">
                <b-navbar-nav class="ml-auto">
                    <b-nav-form>
                        <b-form-input size="sm" class="mr-sm-2" placeholder="Search"></b-form-input>
                        <b-button size="sm" class="my-2 my-sm-0" type="submit">Search</b-button>
                    </b-nav-form>
                    <p>分配item後核可送出</p>
                    <hr />
                    <template>
                        <div>
                            <b-table :items="items" :fields="fields" striped responsive="sm">
                                <template #cell(show_details)="row">
                                    <b-button size="sm" @@click="row.toggleDetails" class="mr-2">
                                        {{ row.detailsShowing ? '隱藏' : '顯示'}}
                                    </b-button>

                                    <!-- As `row.showDetails` is one-way, we call the toggleDetails function on @@change -->
                                    <b-form-checkbox v-model="row.detailsShowing" @@change="row.toggleDetails">
                                        Details via check
                                    </b-form-checkbox>
                                </template>

                                <template #row-details="row">
                                    <b-card>
                                        <b-row class="mb-2">
                                            <b-col sm="3" class="text-sm-right"><b>itemId:</b></b-col>
                                            <b-col>{{ row.item.itemId }}</b-col>
                                        </b-row>

                                        <b-row class="mb-2">
                                            <b-col sm="3" class="text-sm-right"><b>itemSN:</b></b-col>
                                            <b-col>{{ row.item.itemSN }}</b-col>
                                        </b-row>

                                        <b-button size="sm" @@click="row.toggleDetails">隱藏</b-button>
                                    </b-card>
                                </template>
                            </b-table>
                        </div>
                    </template>
</b-tab>
            <b-tab title="待領取">
                <b-navbar-nav class="ml-auto">
                    <b-nav-form>
                        <b-form-input size="sm" class="mr-sm-2" placeholder="Search"></b-form-input>
                        <b-button size="sm" class="my-2 my-sm-0" type="submit">Search</b-button>
                    </b-nav-form>
                    <hr />
                    <template>
                        <div>
                            <b-table :items="items" :fields="fields" striped responsive="sm">
                                <template #cell(show_details)="row">
                                    <b-button size="sm" @@click="row.toggleDetails" class="mr-2">
                                        {{ row.detailsShowing ? '隱藏' : '顯示'}}
                                    </b-button>

                                    <!-- As `row.showDetails` is one-way, we call the toggleDetails function on @@change -->
                                    <b-form-checkbox v-model="row.detailsShowing" @@change="row.toggleDetails">
                                        Details via check
                                    </b-form-checkbox>
                                </template>

                                <template #row-details="row">
                                    <b-card>
                                        <b-row class="mb-2">
                                            <b-col sm="3" class="text-sm-right"><b>itemId:</b></b-col>
                                            <b-col>{{ row.item.itemId }}</b-col>
                                        </b-row>

                                        <b-row class="mb-2">
                                            <b-col sm="3" class="text-sm-right"><b>itemSN:</b></b-col>
                                            <b-col>{{ row.item.itemSN }}</b-col>
                                        </b-row>

                                        <b-button size="sm" @@click="row.toggleDetails">隱藏</b-button>
                                    </b-card>
                                </template>
                            </b-table>
                        </div>
                    </template>
            </b-tab>
            <b-tab title="租借中">
                <p>I'm the tab with the very, very long title</p>
            </b-tab>
            <b-tab title="已結束">
                <p>I'm a disabled tab!</p>
            </b-tab>
            <b-tab title="已逾期">
                <p>I'm a disabled tab!</p>
            </b-tab>
        </b-tabs>
    

   
</div>

<script>
    new Vue({
        el: '#app',
        
        data() {
            return {
                fields: ['OrderId', 'UserId', 'EquipmentId', '設備名稱', 'Quantity', 'EstimatePickupTime', 'Days', '總價', '訂單狀態', '下單時間', 'show_details'],
                items: [
                    //{ itemSN: true, itemId: 40, OrderId: 'Dickerson', UserId: 'Macdonald' },
                    //{ itemSN: false, itemId: 21, OrderId: 'Larsen', UserId: 'Shaw', _showDetails: true },
                    //{ itemSN: false, itemId: 89, OrderId: 'Geneva', UserId: 'Wilson'},
                    //{ itemSN: true, itemId: 38, OrderId: 'Jami', UserId: 'Carney' }
                    {
                        isActive: true, age: 40, first_name: 'Dickerson', last_name: 'Macdonald', loop: [
                            {
                                orderDetailId: 3,
                                itemId: 3,
                                orderDetailStatus: "已領取",

                            },
                            {
                                orderDetailId: 4,
                                itemId: 8,
                                orderDetailStatus: "已領取",

                            }
                    }
                ]
            }
        }
                //beforeCreate: function () {
                //    fetch('OrderApi/GetAllOrders', { method: 'GET' })
                //        .then(response => {
                //            if (response.ok) {
                //                return response.json();
                //            }
                //        })
                //        .then(response => {
                //            this.items = response;
                //        });

                //}
        
        
    })
</script>



